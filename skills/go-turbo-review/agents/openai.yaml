interface:
  display_name: "Go Turbo Review"
  short_description: "Review a Go diff for performance regressions"
  default_prompt: "Use $go-turbo-review to review this Go diff for actionable performance regressions."
policy:
  allow_implicit_invocation: false
