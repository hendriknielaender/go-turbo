---
description: Go performance mode — write and review Go like a staff performance engineer
argument-hint: "[cruise|turbo|redline]"
---

Run the `go-turbo` skill at level `$ARGUMENTS` for the current task (default
`turbo`; valid: `cruise`, `turbo`, `redline`).

Read `skills/go-turbo/SKILL.md`, work down its performance ladder, and apply
only improvements whose preconditions are established. Require representative
evidence before introducing ownership, concurrency, portability, or runtime
complexity. Preserve correctness and report what was not measured.
