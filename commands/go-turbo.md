---
description: Go performance mode — write and review Go like a staff performance engineer
argument-hint: "[cruise|turbo|redline]"
---

Activate the `go-turbo` skill at level `$ARGUMENTS` (default `turbo`; valid: `cruise`, `turbo`, `redline`).

Read `skills/go-turbo/SKILL.md` and apply it to this session: allocation-aware idiomatic Go by default, the ladder worked top-down, free wins applied silently, paid wins only where evidence supports them and always marked with a `turbo:` comment.

Stay active for every subsequent Go response until the user says "stop go-turbo" or "normal mode".
